import React from 'react';

const TierContainer = ({ children }) => (
  <div className="border border-gray-200 rounded-lg shadow-sm divide-y divide-gray-200">
    {children}
  </div>
);

export default TierContainer;
