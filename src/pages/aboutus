import React from 'react';
import { graphql } from 'gatsby';
import {
  SEO,
  InterstitialTitle,
  StickyFooter,
  ResponsiveSpacer,
} from 'components';
// eslint-disable-next-line import/named
import { Hero, FooterCTA } from 'components/home';

const SEO_TITLE = 'Roadie Company About Us';
const HEADLINE = 'Who We Are';
const LEAD = `
Bringing you Backstage for growing engineering teams, deployed in under 5 minutes.
`;

const Home = ({ data, location }) => {
  const siteTitle = data.site.siteMetadata.title;

  return (
    <>
      <SEO title={`${SEO_TITLE} | ${siteTitle}`} description={LEAD} />
      <StickyFooter location={location}>
        <ResponsiveSpacer>
          <Hero siteMetadata={data.site.siteMetadata} headline={HEADLINE} lead={LEAD} />
        </ResponsiveSpacer>

        <ResponsiveSpacer>
          <InterstitialTitle
            id="product"
            size="large"
          >
            Founded in 2020, and having raised a seed round we set out to solve the biggest hurdles of quickly growing engineering teams. We are a small, but quickly growing company. We are fully remote working across Europe.    
          </InterstitialTitle>
        </ResponsiveSpacer>

        <ResponsiveSpacer>
          <InterstitialTitle
            id="solutions"
            size="large"
          >
            Take a bet on us...These folks did!
          </InterstitialTitle>
        </ResponsiveSpacer>

        <ResponsiveSpacer>
          <InterstitialTitle size="large" text="Meet The Team" />
        </ResponsiveSpacer>


        <ResponsiveSpacer>
          <FooterCTA />
        </ResponsiveSpacer>
      </StickyFooter>
    </>
  );
};

export default Home;

export const pageQuery = graphql`
  query {
    site {
      siteMetadata {
        title
        social {
          twitter
        }
      }
    }
  }
`;
